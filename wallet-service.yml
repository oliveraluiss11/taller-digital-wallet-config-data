eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 0
spring:
  kafka:
    bootstrap-server: localhost:9092
  data:
    mongodb:
      uri:  mongodb+srv://oliveraluis11:oliveraluis11@cluster0.iflkfqp.mongodb.net/dw-wallet-service?retryWrites=true&w=majority
